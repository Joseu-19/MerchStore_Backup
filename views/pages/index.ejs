<%- include('../partials/head.ejs') %>
<body>
  <%- include('../partials/menu.ejs') %>
  <!-- flex container -->
  <div class="wrapper d-flex flex-wrap flex-column flex-sm-column flex-md-row flex-lg-row">
  
    <div id="homeBanner"></div>

    <% data.forEach((shirt) => { %> 
     
          <button 
              type="button" 
              class="shirt btn btn-warning" 
              data-meeting-id="<%=shirt.id%>" 
              data-bs-toggle="modal" 
              data-bs-target="#shirtModal">Info
          </button>
      
      </p>
      <% }); %>

  </div>

  <%- include('../partials/modal.ejs') %>

  <script>
    const shirts = <%- JSON.stringify(data) %>;
    console.log(shirts);

    document.querySelectorAll('.shirt').forEach((button, index) => {
        button.onclick = function() {

        };
    });
</script>

</body>
</html>
